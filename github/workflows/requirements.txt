# ─── LLM providers（揀其中一個就夠，全部裝都得）
google-genai>=1.0.0            # Gemini（預設，免費 tier）— 新版 SDK
anthropic>=0.40.0              # Claude（後備）
openai>=1.40.0                 # OpenAI / DeepSeek（OpenAI-compatible）

# ─── Core
feedparser>=6.0.10
requests>=2.31.0
python-dotenv>=1.0.0
opencc-python-reimplemented>=0.1.7
